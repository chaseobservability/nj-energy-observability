# PJM Data Miner 2 (API key needed)
PJM_API_KEY="REPLACE_ME"
PJM_API_BASE="https://api.pjm.com/api/v1"

# Run settings
DEFINITION_VERSION="v1.0"
OUT_DIR="./out"
RAW_DIR="./raw"
STAGE_DIR="./stage"

# Optional (for audit stamping)
CODE_VERSION="git:manual"
